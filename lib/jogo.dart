import 'package:flutter/material.dart';

class PagJogo extends StatelessWidget {
  const PagJogo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('JOGO'),
      ),
      body: Container(),
    );
  }
}
