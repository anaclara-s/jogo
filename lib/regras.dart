import 'package:flutter/material.dart';

class PagReg extends StatelessWidget {
  const PagReg({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('REGRAS'),
      ),
      body: Container(),
    );
  }
}
